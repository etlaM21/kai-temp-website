{% extends 'partials/base.html.twig' %}

{% block content %}
    {% for module in page.collection() %}
        <div id="{{ module.menu|default(module.title)|lower|replace({' ': '-'}) }}" class="module-section">
            {{ module.content|raw }}
        </div>
    {% endfor %}
{% endblock %}
